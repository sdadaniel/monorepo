const add = ({ a, b }: { a: number; b: number }) => {
  return a + b;
};

const subtract = (a: number, b: number) => {
  return a - b;
};
const test = (a: number, b: number) => {
  return a - b;
};

export { subtract, add };
