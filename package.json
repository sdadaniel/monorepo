{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "service/*"
  ],
  "scripts": {
    "lerna:version": "npx lerna version --no-private",
    "lerna:publish": "npx lerna publish --no-private"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.3.0"
  }
}
